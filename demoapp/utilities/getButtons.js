// get buttons from index.html
const discoveryButton = document.getElementById("discoveryButton");
const loginButton = document.getElementById("loginButton");
const tokenButton = document.getElementById("tokenButton");
const tokenRefreshButton = document.getElementById("tokenRefreshButton");
const revokeAccessTokenButton = document.getElementById(
  "revokeAccessTokenButton"
);
const revokeRefreshTokenButton = document.getElementById(
  "revokeRefreshTokenButton"
);
const logoutButton = document.getElementById("logoutButton");
const userinfoCustomerDataButton = document.getElementById(
  "userinfoCustomerDataButton"
);
const userinfoButton = document.getElementById("userinfoButton");
const ssoTicketLoginDataButton = document.getElementById(
  "ssoTicketLoginDataButton"
);
const ssoTicketCustomerDataButton = document.getElementById(
  "ssoTicketCustomerDataButton"
);
const changeLoginDataButton = document.getElementById("changeLoginDataButton");
const changeCustomerDataButton = document.getElementById(
  "changeCustomerDataButton"
);
const registrationButton = document.getElementById("registrationButton");
const passwordResetButton = document.getElementById("passwordResetButton");
const redoLoginButton = document.getElementById("redoLoginButton");
const tokenRefreshCustomerDataButton = document.getElementById(
  "tokenRefreshCustomerDataButton"
);

const buttons = {
  discoveryButton,
  loginButton,
  tokenButton,
  tokenRefreshButton,
  revokeAccessTokenButton,
  revokeRefreshTokenButton,
  logoutButton,
  userinfoCustomerDataButton,
  userinfoButton,
  ssoTicketLoginDataButton,
  ssoTicketCustomerDataButton,
  changeLoginDataButton,
  changeCustomerDataButton,
  registrationButton,
  passwordResetButton,
  redoLoginButton,
  tokenRefreshCustomerDataButton,
};

export default buttons;
